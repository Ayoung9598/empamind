# AWS Cognito Configuration
# Leave empty for local UI testing (auth will not work, but UI will be visible)
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_USER_POOL_CLIENT_ID=

# API Gateway Endpoint
# Leave empty for local UI testing (API calls will fail, but UI will be visible)
VITE_API_ENDPOINT=

# AWS Region
VITE_AWS_REGION=us-east-1

